# 🚀 БЫСТРЫЙ СТАРТ

## За 5 минут к работающему сайту!

### Вариант 1: Без базы данных (самый простой)

1. **Скачайте все файлы** из папки `quiz-app`
2. **Откройте `index.html`** в браузере
3. **Перейдите в админ-панель**: `admin.html`
4. **Добавьте тест** (или используйте `example-test.js`)
5. **Готово!** Проходите викторину

> ⚠️ Данные хранятся только в браузере

---

### Вариант 2: С базой данных Supabase (15 минут)

#### 1️⃣ Создайте проект в Supabase
- Зайдите на https://supabase.com
- Sign up → через GitHub (бесплатно)
- New Project → `history-quiz` → Free plan
- Ждите 2-3 минуты

#### 2️⃣ Создайте таблицы
```sql
-- Скопируйте и выполните в SQL Editor на Supabase:

CREATE TABLE tests (
  id BIGSERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT,
  questions JSONB NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE TABLE user_progress (
  id BIGSERIAL PRIMARY KEY,
  user_id TEXT NOT NULL,
  test_id BIGINT REFERENCES tests(id),
  score NUMERIC(4,1) NOT NULL,
  answers JSONB NOT NULL,
  completed_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Включите доступ:
ALTER TABLE tests ENABLE ROW LEVEL SECURITY;
ALTER TABLE user_progress ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Public access" ON tests FOR SELECT USING (true);
CREATE POLICY "Public insert" ON tests FOR INSERT WITH CHECK (true);
CREATE POLICY "Public access progress" ON user_progress FOR ALL USING (true);
```

#### 3️⃣ Получите ключи
- Settings → API
- Скопируйте:
  * Project URL: `https://xxx.supabase.co`
  * anon public key: `eyJ...`

#### 4️⃣ Обновите конфиг
В файле `supabase-config.js`:
```javascript
const SUPABASE_URL = 'https://xxx.supabase.co'; // ← ваш URL
const SUPABASE_KEY = 'eyJ...'; // ← ваш ключ
```

#### 5️⃣ Тестируйте!
- Откройте `admin.html`
- Добавьте тест
- Проверьте в Supabase → Table Editor → tests

---

### Вариант 3: Публикация онлайн (10 минут)

#### 📦 Vercel (рекомендуется)

1. **GitHub**
   - Создайте репозиторий на https://github.com
   - Загрузите все файлы

2. **Vercel**
   - Зайдите на https://vercel.com
   - Sign up через GitHub
   - Import Project → выберите репозиторий
   - Deploy!

3. **Готово!**
   - Ваш сайт: `https://xxx.vercel.app`
   - Автоматически с HTTPS и CDN

#### 🌐 Или Netlify

- https://netlify.com → Sign up → Deploy → Drag & Drop файлы
- Ваш сайт: `https://xxx.netlify.app`

---

## 📝 Быстрое добавление теста

### Через админ-панель:
1. Откройте `admin.html`
2. Заполните название
3. Добавьте 16 вопросов:
   - 12 закрытых (а,б,в,г)
   - 4 открытых (текст)
4. Сохраните

### Через консоль (быстрее):
1. Откройте `admin.html`
2. F12 → Console
3. Скопируйте код из `example-test.js`
4. Выполните: `API.saveTest(exampleTest)`
5. Готово!

---

## ✅ Проверочный список

- [ ] Все файлы скачаны
- [ ] `index.html` открывается в браузере
- [ ] Supabase настроен (если используете)
- [ ] Ключи добавлены в `supabase-config.js`
- [ ] Первый тест добавлен через `admin.html`
- [ ] Тест проходится без ошибок
- [ ] (Опционально) Сайт опубликован на Vercel

---

## 🆘 Если что-то не работает

1. **Откройте консоль**: F12 → Console
2. **Посмотрите ошибки** (красный текст)
3. **Частые проблемы:**
   - Неправильные ключи Supabase → проверьте URL и KEY
   - Тестов нет → добавьте через админ-панель
   - Файлы не найдены → проверьте структуру папок

---

## 🎯 Что дальше?

1. Добавьте 5-10 тестов для разнообразия
2. Настройте дизайн в `style.css`
3. Поделитесь ссылкой с учениками
4. Следите за результатами в Supabase

**Готово! Ваша викторина работает! 🎉**
