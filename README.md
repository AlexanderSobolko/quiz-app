# 🎯 Историческая Викторина - Руководство по развертыванию

## 📋 Содержание
1. [Быстрый старт (без базы данных)](#быстрый-старт)
2. [Развертывание с Supabase (рекомендуется)](#развертывание-с-supabase)
3. [Публикация на бесплатном хостинге](#публикация-на-хостинге)
4. [Как добавлять новые тесты](#как-добавлять-тесты)
5. [Часто задаваемые вопросы](#faq)

---

## 🚀 Быстрый старт (без базы данных)

Если вы хотите просто запустить сайт локально без настройки базы данных:

### Шаг 1: Подготовка файлов
1. Все файлы уже готовы в папке `quiz-app/`
2. Структура проекта:
```
quiz-app/
├── index.html          # Главная страница
├── quiz.html           # Страница викторины
├── result.html         # Страница результатов
├── admin.html          # Админ-панель
├── style.css           # Стили
├── app.js              # Логика главной страницы
├── quiz.js             # Логика викторины
├── result.js           # Логика результатов
├── admin.js            # Логика админ-панели
└── supabase-config.js  # Конфигурация (работает с localStorage)
```

### Шаг 2: Запуск
1. Откройте `index.html` в браузере
2. Перейдите в админ-панель: `admin.html`
3. Добавьте первый тест (16 вопросов)
4. Вернитесь на главную и начните тест!

**Преимущества:** Работает сразу, не нужна настройка
**Недостатки:** Данные хранятся только в браузере (при очистке кеша - исчезнут)

---

## 🗄️ Развертывание с Supabase (рекомендуется)

Для полноценной работы с базой данных и синхронизацией между устройствами:

### Шаг 1: Создание проекта в Supabase

1. **Зарегистрируйтесь на Supabase**
   - Перейдите на https://supabase.com
   - Нажмите "Start your project"
   - Войдите через GitHub (бесплатно)

2. **Создайте новый проект**
   - Нажмите "New Project"
   - Название: `history-quiz` (любое)
   - Database Password: придумайте пароль (сохраните!)
   - Region: выберите ближайший (например, Europe)
   - Plan: Free (0$)
   - Нажмите "Create new project"
   - Ждите 2-3 минуты (создание базы)

### Шаг 2: Создание таблиц в базе данных

1. **Откройте SQL Editor**
   - В левом меню: "SQL Editor"
   - Нажмите "New query"

2. **Создайте таблицу для тестов**
   Скопируйте и выполните этот SQL:
```sql
-- Таблица тестов
CREATE TABLE tests (
  id BIGSERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT,
  questions JSONB NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Включаем Row Level Security
ALTER TABLE tests ENABLE ROW LEVEL SECURITY;

-- Политика: все могут читать
CREATE POLICY "Anyone can read tests"
  ON tests FOR SELECT
  USING (true);

-- Политика: только аутентифицированные могут добавлять
CREATE POLICY "Authenticated users can insert tests"
  ON tests FOR INSERT
  WITH CHECK (true);
```

3. **Создайте таблицу для прогресса пользователей**
```sql
-- Таблица прогресса пользователей
CREATE TABLE user_progress (
  id BIGSERIAL PRIMARY KEY,
  user_id TEXT NOT NULL,
  test_id BIGINT REFERENCES tests(id),
  score NUMERIC(4,1) NOT NULL,
  answers JSONB NOT NULL,
  completed_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Включаем Row Level Security
ALTER TABLE user_progress ENABLE ROW LEVEL SECURITY;

-- Политика: пользователи видят только свой прогресс
CREATE POLICY "Users can read own progress"
  ON user_progress FOR SELECT
  USING (true);

-- Политика: пользователи могут добавлять свой прогресс
CREATE POLICY "Users can insert own progress"
  ON user_progress FOR INSERT
  WITH CHECK (true);

-- Индекс для быстрого поиска
CREATE INDEX idx_user_progress_user_id ON user_progress(user_id);
CREATE INDEX idx_user_progress_test_id ON user_progress(test_id);
```

4. Нажмите "Run" (или Ctrl+Enter)

### Шаг 3: Получение ключей API

1. **Откройте настройки проекта**
   - В левом меню: "Settings" → "API"

2. **Скопируйте два значения:**
   - `Project URL` (например: `https://abcdefgh.supabase.co`)
   - `anon public` ключ (длинная строка)

3. **Обновите файл `supabase-config.js`**
   Откройте файл и замените:
```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL'; // → вставьте ваш URL
const SUPABASE_KEY = 'YOUR_SUPABASE_ANON_KEY'; // → вставьте ваш ключ
```

### Шаг 4: Тестирование

1. Откройте `admin.html` в браузере
2. Добавьте тест
3. Откройте консоль браузера (F12)
4. Проверьте, что нет ошибок
5. Пройдите тест - данные должны сохраниться в Supabase!

---

## 🌐 Публикация на хостинге

После настройки Supabase, опубликуйте сайт бесплатно:

### Вариант 1: Vercel (Рекомендуется)

**Преимущества:**
- Самый простой деплой
- Автоматические HTTPS
- CDN по всему миру
- Бесплатный план навсегда

**Шаги:**

1. **Создайте аккаунт GitHub** (если нет)
   - Перейдите на https://github.com
   - Sign up → бесплатная регистрация

2. **Создайте репозиторий**
   - Нажмите "+" → "New repository"
   - Название: `history-quiz`
   - Public
   - "Create repository"

3. **Загрузите файлы**
   - Нажмите "uploading an existing file"
   - Перетащите все файлы из папки `quiz-app/`
   - "Commit changes"

4. **Деплой на Vercel**
   - Перейдите на https://vercel.com
   - "Sign up" → войдите через GitHub
   - "Import Project"
   - Выберите ваш репозиторий `history-quiz`
   - "Deploy"
   - Ждите 1-2 минуты

5. **Готово!**
   - Ваш сайт: `https://history-quiz.vercel.app`
   - Можете настроить свой домен в настройках

### Вариант 2: Netlify

1. Перейдите на https://netlify.com
2. "Sign up" → через GitHub
3. "Add new site" → "Import an existing project"
4. Выберите GitHub → ваш репозиторий
5. "Deploy site"
6. Готово! Сайт доступен по адресу `https://вашсайт.netlify.app`

### Вариант 3: GitHub Pages

1. В вашем репозитории на GitHub:
2. Settings → Pages
3. Source: "Deploy from a branch"
4. Branch: "main" → "/ (root)" → Save
5. Ваш сайт: `https://ваш-логин.github.io/history-quiz`

---

## 📝 Как добавлять тесты

### Через админ-панель (UI)

1. **Откройте админ-панель**
   - Перейдите на `admin.html` (или `/admin.html` на вашем сайте)

2. **Заполните информацию о тесте**
   - Название: "История Беларуси XVI-XVIII вв"
   - Описание: "Тест по параграфам 26-28"

3. **Добавьте вопросы (16 штук)**
   - Нажмите "+ Добавить вопрос"
   - Введите текст вопроса
   - Выберите тип:
     * **С вариантами** - для закрытых вопросов (а,б,в,г)
     * **Текстовый** - для открытых вопросов

4. **Для закрытых вопросов:**
   ```
   Текст: "Именно в этом году произошел 1 раздел Речи Посполитой"
   А: 1767
   Б: 1768
   В: 1772  ← правильный
   Г: 1788
   ```

5. **Для открытых вопросов:**
   ```
   Текст: "Именно так назывался созданный в 1775 г орган власти"
   Правильные ответы: Постоянная Рада, Рада
   (через запятую - все возможные варианты)
   ```

6. **Добавьте все 16 вопросов**
   - 12 закрытых (с вариантами)
   - 4 открытых (текстовые)

7. **Сохраните тест**
   - Нажмите "💾 Сохранить тест"
   - Проверьте, что тест появился в списке

### Как работает ротация тестов

**Автоматическая ротация:**
- Система отслеживает, какие тесты пользователь уже прошел
- При нажатии "Начать тест" выбирается **случайный непройденный** тест
- Когда все тесты пройдены → начинается с начала (бесконечный цикл)

**Преимущества:**
- Пользователь не видит один и тот же тест подряд
- Можно создать библиотеку из 10-20 тестов
- Каждый раз новый опыт

**Пример:**
```
У вас есть тесты: A, B, C
Пользователь прошел: A, B
Следующий тест: C (единственный непройденный)

После прохождения C:
Все пройдены → сброс → снова доступны A, B, C
```

---

## 🎨 Настройка дизайна

### Изменение цветов

Откройте `style.css` и измените переменные:

```css
:root {
    --primary-gold: #ffd700;    /* Золотой */
    --dark-gold: #ff8c00;       /* Темно-золотой */
    --deep-blue: #0a0e27;       /* Фон */
    --navy: #151b3d;            /* Карточки */
    --success: #00ff88;         /* Правильно */
    --error: #ff4757;           /* Неправильно */
}
```

### Изменение шрифтов

```css
/* В начале style.css замените: */
@import url('https://fonts.googleapis.com/css2?family=ВашШрифт&display=swap');
```

---

## ❓ FAQ (Часто задаваемые вопросы)

### Q: Можно ли использовать без интернета?
**A:** Да! Если не настраивать Supabase, сайт работает полностью офлайн через `localStorage`. Просто откройте `index.html` в браузере.

### Q: Как сделать, чтобы только я мог добавлять тесты?
**A:** 
1. Вариант простой: не давайте ссылку на `admin.html` другим
2. Вариант с защитой: добавьте простую аутентификацию в `admin.html`:
```javascript
const password = prompt('Введите пароль:');
if (password !== 'ваш_пароль') {
    alert('Неверный пароль!');
    window.location.href = 'index.html';
}
```

### Q: Можно ли добавить картинки в вопросы?
**A:** Да! Отредактируйте код:
```javascript
// В quiz.js, функция displayQuestion():
question.text = question.text + '<br><img src="' + question.image + '" style="max-width: 100%;">';
```

### Q: Как экспортировать результаты в Excel?
**A:** Добавьте эту функцию в `admin.js`:
```javascript
async function exportResults() {
    const results = await API.getUserProgress();
    const csv = results.map(r => `${r.user_id},${r.test_id},${r.score}`).join('\n');
    const blob = new Blob([csv], { type: 'text/csv' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = 'results.csv';
    a.click();
}
```

### Q: Мой сайт не работает после публикации!
**A:** Проверьте:
1. Открыть консоль браузера (F12) → вкладка "Console"
2. Есть ли ошибки красным цветом?
3. Проверьте, что в `supabase-config.js` указаны правильные URL и KEY
4. Проверьте, что все файлы загружены на хостинг

### Q: Как добавить таймер на вопросы?
**A:** В `quiz.js` добавьте:
```javascript
let timeRemaining = 30; // секунд на вопрос

function startTimer() {
    const timer = setInterval(() => {
        timeRemaining--;
        document.getElementById('timer').textContent = timeRemaining;
        
        if (timeRemaining <= 0) {
            clearInterval(timer);
            // Автоматически переход к следующему вопросу
            nextQuestion();
        }
    }, 1000);
}
```

### Q: Можно ли использовать Python вместо JavaScript?
**A:** Можно! Но для бесплатного хостинга JavaScript проще. Если хотите Python:
- Backend: Flask или FastAPI
- База данных: PostgreSQL (тоже бесплатно на Supabase)
- Хостинг: Render.com или Railway.app (бесплатные планы)

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль браузера (F12 → Console)
2. Убедитесь, что все файлы на месте
3. Проверьте, что Supabase настроен правильно
4. Попробуйте очистить кеш браузера (Ctrl+Shift+Delete)

---

## 🎉 Готово!

Ваш сайт-викторина готов к использованию!

**Быстрая проверка:**
✅ Создали проект в Supabase
✅ Создали таблицы в базе данных
✅ Обновили ключи в `supabase-config.js`
✅ Опубликовали на Vercel/Netlify
✅ Добавили первый тест через админ-панель
✅ Прошли тест и увидели результаты

**Следующие шаги:**
1. Добавьте 5-10 тестов для разнообразия
2. Поделитесь ссылкой с учениками
3. Следите за статистикой в Supabase

Удачи! 🚀
